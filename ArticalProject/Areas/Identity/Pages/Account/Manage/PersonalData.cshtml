@page
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model PersonalDataModel
@{
    ViewData["Title"] = "البيانات الشخصية";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3 class="text-center my-4">@ViewData["Title"]</h3>

<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <p>يحتوي حسابك على البيانات الشخصية التي قدمتها لنا. تتيح لك هذه الصفحة تنزيل تلك البيانات أو حذفها.</p>
                <p>
                    <strong>حذف هذه البيانات سيؤدي إلى إزالة حسابك بشكل دائم، ولا يمكن استعادته.</strong>
                </p>
                <form id="download-data" asp-page="DownloadPersonalData" method="post" class="mb-3">
                    <button class="btn btn-primary w-100" type="submit">
                        <i class="fas fa-download"></i> تنزيل
                    </button>
                </form>
                <p>
                    <a id="delete" asp-page="DeletePersonalData" class="btn btn-danger w-100">
                        <i class="fas fa-trash-alt"></i> حذف
                    </a>
                </p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}