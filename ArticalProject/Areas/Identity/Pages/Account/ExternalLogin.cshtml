@page
@using Microsoft.AspNetCore.Mvc.TagHelpers
@model ExternalLoginModel
@{
    ViewData["Title"] = "التسجيل";
}

<h1>@ViewData["Title"]</h1>
<h2 id="external-login-title">ربط حسابك @Model.ProviderDisplayName.</h2>
<hr />

<p id="external-login-description" class="text-info">
    لقد قمت بتوثيق الدخول بنجاح باستخدام <strong>@Model.ProviderDisplayName</strong>.
    يرجى إدخال عنوان بريدك الإلكتروني لهذا الموقع أدناه والنقر على زر التسجيل لإنهاء
    عملية تسجيل الدخول.
</p>

<div class="row">
    <div class="col-md-4">
        <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.Email" class="form-control" autocomplete="email" placeholder="يرجى إدخال بريدك الإلكتروني." />
                <label asp-for="Input.Email" class="form-label"></label>
                <span asp-validation-for="Input.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">التسجيل</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
